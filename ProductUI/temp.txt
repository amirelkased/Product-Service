<nav class="bg-dark text-light p-4 shadow-md">
  <div class="container mx-auto flex justify-between items-center">
    <!-- Logo and navigation -->
    <div class="flex items-center space-x-6">
      <a
        [routerLink]=""
        class="text-white text-xl font-semibold hover:text-gray-300"
      >
        <img [src]="userImg" alt="MyStore Logo" class="h-8 w-auto" />
      </a>

      <!-- Navigation Links -->
      <a
        [routerLink]="['']"
        class="text-gray-400 hover:text-white transition-colors duration-300"
        aria-label="Home"
      >
        Home
      </a>
      <a
        *ngIf="isAdmin"
        [routerLink]="['products/new']"
        class="text-gray-400 hover:text-white transition-colors duration-300"
        aria-label="New Product"
      >
        New Product
      </a>
    </div>

    <!-- User Profile Button -->
    <div class="relative">
      <button
        class="text-white bg-red-700 hover:bg-red-600 rounded-full h-10 w-10 flex items-center justify-center transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-red-500"
        aria-label="User Profile"
      >
        <!-- Replace with user icon/profile image -->
        <span class="text-sm font-medium">U</span>
      </button>

      <!-- Dropdown for user actions (optional) -->
      <!-- Uncomment if needed
      <div class="absolute right-0 mt-2 w-48 py-2 bg-white rounded-md shadow-lg z-20">
        <a href="#" class="block px-4 py-2 text-gray-800 hover:bg-gray-100">Profile</a>
        <a href="#" class="block px-4 py-2 text-gray-800 hover:bg-gray-100">Logout</a>
      </div>
      -->
    </div>
  </div>
</nav>
